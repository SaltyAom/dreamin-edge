import { FunctionComponent, VNode } from 'preact'

interface StoreLayoutProps {
    children: VNode
}

type StoreLayoutComponent = FunctionComponent<StoreLayoutProps>
export default StoreLayoutComponent